# Kalistar V4

- Read `../docs/GUIDE_REPRISE.md` for the handoff, current source priority and UX requirements. Read `docs/IDENTITE_VISUELLE.md` for the consolidated visual decisions and `docs/REGLES_JEU.md` for mechanics, stats and documented implementation differences. Never silently resolve a design/code discrepancy by changing the game.
- Read `DIRECTION_ARTISTIQUE.md` before creating or revising character artwork. Matching the approved Momo / Valazar painterly identity is a user-mandated golden rule.
- Generate the illustration separately from the card. Never regenerate the complete validated card frame to make a new character.
- The active frame is the 897 x 1497 px stable template, not the historical 950 x 1655 px experiments in `master/`.
- Preserve approved Momo and Taulio files. New characters use versioned PSD/PNG outputs, editable native text, separate effects and embedded smart objects.
- Use `template-stable/current.json` and the self-contained 03F master for the five-card Electro route. The user-confirmed 2026-09-17 cleanup removed superseded PSDs and historical QA exports; see `../maintenance/cleanup-2026-09-17/RAPPORT.md`. Preserve these PSDs/PNGs and profiles as well as all approved cards in the current reference lock. Earlier migration scripts describe historical stages, not current production dependencies.
- Preserve V3 mechanics unless the user explicitly requests a gameplay change. Do not modify V3 site or database during artwork iterations.
- The first non-Electro series uses `template-stable/current-elements.json`, the 04 multi-element master and `elements-01/*/card.json`. Read `template-stable/elements-01/README.md`. The 03F master and `current.json` remain the preserved Electro route. Never use the historical card snapshots in the preparation manifest instead of the current per-card profiles.
- Verify fixed-frame pixel identity, reopened PSD rendering, number fields, small-size readability and the actual rendered barcode before delivery.
- The remaining families use `template-stable/elements-02/*/card.json`, registry 05 and the same 04 master. Read `template-stable/elements-02/README.md`. `current-elements.json` combines both non-Electro series; select the batch-specific renderer. GEO means Terre, MINERO means Roche. NONE keeps the unlit hexagon and must have no magic or barriers. Preserve approved illustrations unless explicitly asked to change them.
- For effect icons, calibrate the visible motif against the actual painted inner rim, not the image bounds or numeric-field anchor. Store optical anchors in the registry, test the entire alpha contour inside the circle with breathing room, and verify repeat rendering is pixel-stable. Check the final enlarged and small-size render visually; geometric tests alone are not approval.
- The non-Electro branch contour was revised on 2026-09-18 in the active 04 master and all 21 non-Electro PSDs/PNGs. Read `revisions/2026-09-18-branches/README.md`. The corrected source is embedded; never restore the historical truncated branch extraction. Electro remains unchanged. `current-elements.json.frameRevision` and the Atelier reference lock identify this deliberate revision; previous files are preserved under the revision's `originals/`. Never reset the Atelier lock to hide a failed comparison.
- The playable V4 site is `site/`, served by `atelier/server.cjs` at `/jeu/`, with an embedded Atelier tab. Its catalogue is strictly the approved V4 models in the current `atelier/data/references.json` registry plus verified publications from `donnees/catalogue.json`; derive counts from that registry and keep V3 files and browser storage separate. Read `atelier/README.md` before changing the designer. Components are locked by `atelier/designer-assets/manifest.json` and its hashes. New cards go to `creations/<model-id>/`; never replace approved sources. QA jobs must be marked test-only from creation and cannot be published. Keep native numeric/identity text editable and component imports as smart objects.
- The Balmhyr / Lok icon revision is documented in `revisions/2026-09-18-balmhyr-icons/README.md`. Once published, `template-stable/icon-layouts.json` supplies native optical layouts for Hache and NAIN; `elements-common.jsx` and the matching designer banks must preserve them. Use the visible motif's optical anchor and its full alpha contour inside the medallion, never the older rectangular 87 px NAIN fit alone. Balmhyr's active artwork is the versioned Canyonero illustration; original art and PSDs remain in the revision backup.
- Voloden's canonical V3-to-V4 conversion is documented in `revisions/2026-09-18-voloden/README.md`. His profile is `template-stable/voloden/card.json`, preserving ID `30000028`, artwork and gameplay. This approved reference is distinct from user-created Atelier cards. The native Faucille / CARDEMORTIS layouts and designer banks were added without changing existing components. Further canonical additions must use an explicit additive reference publication, preserve previous identities and pass the full native regression; never rerun the historical freeze script.
- The weapon medallion's small upper-left white fragment is corrected in `revisions/2026-09-20-weapon-rim/`. Use the revised embedded lower frame and Atelier frame images; do not move the weapon icon to conceal it. The revision preserves every pixel outside the medallion repair, with unchanged text, positions and gameplay. Consult its publication and regression reports before reusing historical proofs.
